<h1>ジャンル編集</h1>
<div class="form-group row">
  <%= form_with model: [:admin, @genre] do |f| %>
    <label for="genre" class="col-sm-5 col-form-label">ジャンル名</label>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存', class: 'btn btn-success' %>
  <% end %>
</div>